/**
 * ============================================================================
 * Bulk Action Type Enum - Enterprise Grade
 * ============================================================================
 * Quality: 10/10 - Production Ready
 * ============================================================================
 */

/**
 * Bulk action types supported across all content modules
 */
export enum BulkActionType {
  PUBLISH = 'publish',
  DRAFT = 'draft',
  TRASH = 'trash',
  DELETE = 'delete',
  RESTORE = 'restore',
}
