/**
 * ============================================================================
 * Content Status Enum - Enterprise Grade
 * ============================================================================
 * Quality: 10/10 - Production Ready
 * ============================================================================
 */

/**
 * Content status enum for all content types
 * Used for content lifecycle management (draft, publish, trash)
 */
export enum ContentStatus {
  PUBLISHED = 'published',
  DRAFT = 'draft',
  TRASH = 'trash',
}
